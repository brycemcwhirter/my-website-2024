---
import MySiteLayout from "../layouts/MySiteLayout.astro"
import BlockLayout from "../layouts/BlockLayout.svelte"

let description = "I'm a bedroom producer by night. Here is where you can see some music that I've made.";
let pageType = "Music";
let buttonMessage = "See Dropbox File"
import projects from '../data/music_projects'

---

<MySiteLayout title="Music">
    <BlockLayout {pageType} {description} {projects} {buttonMessage}/>

</MySiteLayout>